# Ocorrencias
> Decidi terminar tudo hoje mesmo, (25) só pra ver se alegra um pouco essa quinta feira " = D "

# Quebrar pip
> Eu tava olhando alguns fóruns, etc, e creio que esse comando funciona sem ter que ficar fazendo diversas instalações para executar bibliotecas
```bash
pip install {biblioteca} --break-system-packages
